services:
  llama-server:
    volumes:
      - ./models:/models
    image: ghcr.io/ggml-org/llama.cpp:server
    ports:
      - 8080:8080
    command:
      - "--host"
      - "0.0.0.0"
      - "--port"
      - "8080"
      - "--embeddings"
      - "--models-dir" # Enables multiple models on one server
      - "/models"
      - "--models-max"
      - "1"
      - "-c"
      - "2048"
      - "--n-cpu-moe" # Offloads model processing to CPU
      - "12"
      - "--mlock" # Forces the system to keep model in RAM rather than swapping or compressing
      - "--verbose"

  weaviate:
    volumes:
      - vector-db:/vector-db
    command:
      - --host
      - 0.0.0.0
      - --port
      - "8081"
      - --scheme
      - http
    image: cr.weaviate.io/semitechnologies/weaviate:1.37.7
    ports:
      - 8081:8081
      - 50051:50051
    restart: on-failure:0
    environment:
      QUERY_DEFAULTS_LIMIT: 8
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: "true"
      PERSISTENCE_DATA_PATH: "/vector-db"
      ENABLE_MODULES: "text2vec-openai,generative-openai"
      OPENAI_APIKEY: "not-needed"
      OPENAI_BASEURL: "http://localhost:8080/"
      CLUSTER_HOSTNAME: "node1"

volumes:
  vector-db:
